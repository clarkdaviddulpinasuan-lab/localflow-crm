# Placeholders keep src/lib/supabase.ts importable on CI where .env is absent.
# Services are mocked in src/test/supabaseMock.ts, so these values are never used.
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=ci-placeholder-anon-key
VITE_APP_NAME=LocalFlow CRM
VITE_MESSAGE_PROVIDER=dryrun