# Production build configuration.
# These are all browser-public values (they ship in the client bundle anyway),
# so committing them is safe. Vite loads this file automatically during
# `vite build`, including Vercel's cloud build — no dashboard env vars needed.
VITE_SUPABASE_URL=https://sczxmdvqgdboczxdmwjt.supabase.co
# The sb_publishable_ prefix is intentional: it tells Supabase's JS client
# this anon key is approved for browser use. Do NOT remove the prefix.
VITE_SUPABASE_ANON_KEY=sb_publishable_Ymsjtql2F5F0Ozy49Z2GOA_FpB_7kbv

# Application
VITE_APP_NAME=LocalFlow CRM
VITE_DEMO_MODE=false
