# Copied by the Supabase CLI when running `supabase functions serve`
# so the send-message / send-test-email functions can reach Resend.
# Get a key from https://resend.com/api-keys (must own a verified sending domain
# for the `from_email` you configure in the app's Settings → Messaging).
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx